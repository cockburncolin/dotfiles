# -*- mode: snippet -*-
# name: Nix Module
# key: <m
# --
{config, lib, pkgs, ...}:
let
	cfg = config.custom.${1:Module Name};
in
{
	options = {
		custom.$1.enable = lib.mkEnableOption "$2";
	};

	config = lib.mkIf cfg.enable {
	       $0
	};
}